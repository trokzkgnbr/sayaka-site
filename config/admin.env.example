# Blog 管理サーバー用（Git にコミットしない）
# 初回: bash scripts/setup_admin_password.sh

# scripts/setup_admin_password.sh が生成（平文パスワードは保存しない）
ADMIN_PASSWORD_HASH=

# セッション署名用（自動生成）
SESSION_SECRET=

# ローカルだけで listen（他社から直接アクセス不可）
ADMIN_BIND=127.0.0.1
ADMIN_PORT=8765
